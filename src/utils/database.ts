import { Db, MongoClient } from 'mongodb';
import Database from '../dbConfigs';

export class appdb {
    //const connect: Db = (await Database.dbConnection()).db('app');
    // function init() {
        
    // }
}